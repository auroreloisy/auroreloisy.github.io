Based on https://academicpages.github.io/ and customized.

Color themes are defined in `_sass\theme` (with dark and light variants). The choice is made in `_config.yml` (site_theme). At the moment only one theme is defined.


